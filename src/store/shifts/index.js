import * as actions from './shifts.actions';
import * as types from './shifts.types';
import * as epics from './shifts.epics';
import reducer from './shifts.reducer';

export default {
    actions,
    types,
    epics,
    reducer
};